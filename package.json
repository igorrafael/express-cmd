{
  "name": "express-cmd",
  "version": "0.1.0",
  "description": "URL centric API for runing subprocesses using an express router",
  "main": "lib/index.js",
  "scripts": {
    "build": "coffee -o lib/ -c src/",
    "postinstall": "npm run build",
    "prepublish": "npm run build",
    "pretest": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "coffee-script": "^1.10.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/igorrafael/express-cmd"
  },
  "keywords": [
    "express", "shell", "bash", "command", "cmd"
  ],
  "author": "Igor Rafael de Sousa",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/igorrafael/express-cmd/issues"
  },
  "dependencies": {
    "lodash": "~4.13.1",
    "chai": "~3.5.0",
    "mocha": "~2.5.3",
    "express": "~4.14.0",
    "nodemon": "~1.9.2"
  }
}
